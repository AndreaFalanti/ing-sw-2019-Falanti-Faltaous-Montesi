1. Refactor Expression hierarchy to expose only one eval-like method (evalToEvalResult probably)
2. Refactor Expression hierarchy to introduce AtomicExpression and ListExpression
    a. Create hierarchy
    b. Change internal representation of subexpression in ListExpression from map to array
        I. Change pretty parser to respect this new rep
